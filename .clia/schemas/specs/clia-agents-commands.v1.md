# CLIA Agents — Command Matrix (v1)

Status: draft

- `clia agents commission` — typed commissioning (dry‑run/full)
- `clia agents mirrors render` — write mirrors under `.generated/`
- `clia agents audit self` — validate triads (handle/expertise/slug)
- `clia agents transfer` — in‑place rename and directory moves with receipts
- `clia agents journal write` — write JSON journal (date+timestamp; persona signature)
- (future) `clia agents whoami` — print active persona/slug for confirmation
